import numpy as np
my_list=[1,2,3,4,5]
print(my_list)
print(type(my_list))

#1. In array elements are stored in a memory and storage is continuos whereas storage in list are not contnious
# 2. 
array=np.array([1,2,3,4,5])
print(array)
print(type(array))